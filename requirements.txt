# Runtime dependencies (pinned for reproducible builds).
# Dev/test tooling lives in requirements-dev.txt.

# --- Web framework ---
Django==4.2.30
djangorestframework==3.17.1
python-dotenv==1.2.2

# --- ML ---
pandas==2.3.3
scikit-learn==1.9.0
joblib==1.5.3
numpy==2.4.0

# --- Production server + static serving ---
gunicorn==26.0.0
whitenoise==6.12.0

# --- LLM (LangChain) ---
langchain==1.3.7
langchain-openai==1.3.0   # OpenAI + DeepSeek (OpenAI-compatible)

# Optional LLM providers — install only the one whose API key you set:
#   pip install langchain-anthropic     # Anthropic Claude
#   pip install langchain-groq          # Groq (llama3, mixtral)
#   pip install langchain-google-genai  # Google Gemini
